import { Box } from "@mui/material";
import styled from "styled-components";

export const SmallerSeparatorContainer = styled(Box)`
    border-top: 0.5px solid white;
    margin: 36px auto;
    width: calc(100% - 32px);
`